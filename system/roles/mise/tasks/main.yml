---
- name: Install mise packages
  ansible.builtin.package:
    name: "{{ mise_packages }}"
    state: present
  become: true

- name: Sets up rust (Ruby requirement)
  ansible.builtin.import_role:
    name: rust
