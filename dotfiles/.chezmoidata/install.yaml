install:
  packages:
    pacman:
      ## Base
      - base-devel # yay
      # Dell
      - libsmbios
      # Firewall
      - ufw
      # Password Manager
      # VMs
      - dnsmasq
      - dmidecode
      - libvirt
      - qemu-full
      - virt-manager
      # Dev Env (Mise)
      ## IA tools
      ## Docker
      - docker
      - docker-buildx
      - docker-compose
      - nvidia-container-toolkit
      - lazydocker
      ## Infrastructure
      ## Other
  # - chezmoi
